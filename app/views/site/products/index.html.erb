<div class="container">
  <div class="row">
    <% @products.each do |product| %>
    <div class="col-md-6">
      <h2><%= product.name %></h2>
      <p><%= product.abstract %></p>
      <a href="" class="btn btn-primary">了解更多 »</a>
    </div>
    <% end %>
  </div>
</div>
