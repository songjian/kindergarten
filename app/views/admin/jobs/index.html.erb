<section class="content-header">
  <h1>
    职位管理
    <small>职位列表</small>
  </h1>
  <ol class="breadcrumb">
    <li>
      <%= link_to admin_path do %>
        <i class="fa fa-home"></i> 首页
      <% end %>
    </li>
    <li>职位管理</li>
    <li class="active">列表</li>
  </ol>
</section>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box">
        <div class="box-header">
          <h3 class="box-title">
            <div class="btn-group">
              <%= link_to '新增', new_admin_job_path, class: 'btn btn-default btn-xs' %>
            </div>
          </h3>

          <div class="box-tools">

            <%= search_form_for [:admin, @q] do |f| %>
              <div class="input-group input-group-sm" style="width: 150px;">
                <%= f.search_field :name_cont, class: 'form-control pull-right', placeholder: '招聘职位' %>

                <div class="input-group-btn">
                  <button type="submit" class="btn btn-default"><i class="fa fa-search"></i></button>
                </div>
              </div>
            <% end %>
          </div>
        </div>

        <div class="box-body table-responsive no-padding">
          <table class="table table-hover">
            <tr>
              <th>招聘职位</th>
              <th>职位描述</th>
              <th>操作</th>
            </tr>
            <% @jobs.each do |job| %>
            <tr>
              <td><%= job.name %></td>
              <td><%= job.description %></td>
              <td>
                <%= link_to edit_admin_job_path(job), class: 'btn btn-primary btn-xs' do %>
                  <i class="fa fa-edit"></i> 编辑
                <% end %>
              </td>
            </tr>
            <% end %>
          </table>
        </div>
        <%= paginate @jobs, views_prefix: 'admin' %>
      </div>
    </div>
  </div>
</section>
